using System;
using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;
using Random = System.Random;


public class Helper : MonoBehaviour
{

    // public ParticleSystem particleSystem;
    // public Transform spawnTransform;
    void Start()
    {
    }

    void Update()
    {
        /*if (Input.GetKeyDown(KeyCode.G))
        {
            var ps = Instantiate(particleSystem, spawnTransform.position, Quaternion.identity);
            ps.Play();
            // playerAttack.CurrentWeapon.impactParticle.Play();

        }*/
    }
    
    /*foreach (var item in other.contacts)
    {
        Debug.DrawRay(item.point, item.normal * 100, UnityEngine.Random.ColorHSV(0f, 1f, 1f, 1f, 0.5f, 1f), 10f);
    }*/
}
using System;
using System.Collections;
using System.Collections.Generic;
using Player;
using UnityEngine;
using UnityEngine.InputSystem;

public class PlayerManager : MonoBehaviour
{
    //REFERENCES
    // public CharacterAnimator PlayerAnimator { get; private set; }
    // public InventoryHolder InventoryHolder { get; private set; }

    //FLAGS
    // public bool isGrounded;
    // private bool isInteracting;


    //temp
    // public bool IsInteracting => PlayerAnimator.animator.GetBool(CharacterAnimator.IsInteractingAnim);

    private void Awake()
    {
        // InputManager = GetComponent<InputManager>();
        // PlayerAnimator = GetComponent<CharacterAnimator>();
        // PlayerAttack = GetComponent<PlayerAttack>();
        // InventoryHolder = GetComponent<InventoryHolder>();
        //PlayerLocomotion = GetComponent<PlayerLocomotion>();
    }
}